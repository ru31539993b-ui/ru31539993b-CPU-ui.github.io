# AGENTS.md

兒童遊戲入口網站，純靜態 HTML/CSS/JS（無框架、無建置工具、無相依套件）。

- 直接瀏覽 `index.html` 即可使用
- 共用樣式放在 `style.css`，各頁面透過 `<link>` 引用
- 遊戲頁面：
  - `memory.html` — 4×4 記憶翻牌配對
  - `math.html` — 加減法算術練習（三種難度）
  - `puzzle.html` — 3×3 滑塊拼圖（8 格動物 emoji）
- 無需安裝、無需伺服器、無測試指令
